# Local GitHub OAuth application credentials used by `supabase start`.
# Configure the GitHub OAuth application's callback as:
# http://localhost:54321/auth/v1/callback
SUPABASE_AUTH_EXTERNAL_GITHUB_CLIENT_ID=replace_with_github_oauth_client_id
SUPABASE_AUTH_EXTERNAL_GITHUB_SECRET=replace_with_github_oauth_client_secret
